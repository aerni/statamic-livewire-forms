<input
    id="{{ $this->honeypot->id }}"
    name="{{ $this->honeypot->id }}"
    type="text"
    wire:model="{{ $this->honeypot->key }}"
    tabindex="-1"
    autocomplete="off"
    class="hidden"
    aria-hidden="true"
/>
